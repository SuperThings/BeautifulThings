#include <stdio.h>
#include <cs50.h>

int main(void)
{
    int counter = 0;
    int n = 100;
            while (counter < n)
            {
                counter +=1;
                printf("The count is now %i.\n", counter);
            if (counter == 100)
            {
                printf("You have reached the Max level 100.\n");
            }
            }


}